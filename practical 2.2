#include <stdio.h>
int main ()
 {

Int a, b, c;

Printf("Enter three numbers: ");
scanf ("%d %d %d", &a, &b, &c);

If (a>=b &&a=c)

Printf("a is the largest.");
else if (b >=a &&b>=C)

Printf("b is the largest.");
else

Pointf("c is the largest.");
return 0;
}

#include <stdio.h>

int main() {
    FILE *fp = fopen("sample.txt", "r");
    char ch;

    fseek(fp, 0, SEEK_END);

    for(int i = ftell(fp)-1; i >= 0; i--) {
        fseek(fp, i, 0);
        ch = fgetc(fp);
        printf("%c", ch);
    }

    fclose(fp);
    return 0;
}
